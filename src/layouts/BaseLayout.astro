---
const { title } = Astro.props;
---
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title ? `${title} | AutoFix` : "AutoFix Taller Mecánico"}</title>
  </head>
  <body class="bg-gray-50 text-gray-800 font-sans">
    <header class="bg-blue-700 text-white p-4 shadow-lg sticky top-0 z-50">
      <nav class="container mx-auto flex justify-between items-center">
        <h1 class="text-2xl font-bold">🔧 AutoFix</h1>
        <div class="flex space-x-6 items-center">
          <a href="#inicio" class="hover:text-yellow-300">Inicio</a>
          
          <!-- Dropdown de Servicios -->
          <div class="relative group">
            <a href="#servicios" class="flex items-center gap-1 hover:text-yellow-300 transition">
              Servicios
              <svg class="w-4 h-4 transition-transform group-hover:rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
              </svg>
            </a>
            
            <!-- Menú desplegable -->
            <div class="absolute top-full left-0 mt-2 w-48 bg-white text-gray-800 rounded-lg shadow-xl opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200">
              <a href="pintura" class="block px-4 py-3 hover:bg-blue-50 transition rounded-lg">
                 Pintura
              </a>
            </div>
          </div>

          <a href="#contacto" class="hover:text-yellow-300">Contacto</a>
          <a href="#precios" class="hover:text-yellow-300">Precios</a>
        </div>
      </nav>
      
    </header>

    <main class="container mx-auto px-4 py-8">
      <slot />
    </main>

    <footer class="bg-gray-900 text-gray-300 text-center py-6 mt-10">
      <p>© {new Date().getFullYear()} AutoFix. Todos los derechos reservados.</p>
    </footer>
  </body>
</html>