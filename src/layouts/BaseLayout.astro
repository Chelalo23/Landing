---
const { title } = Astro.props;
---
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title ? `${title} | AutoFix` : "AutoFix Taller Mecánico"}</title>
  </head>
  <body class="bg-gray-50 text-gray-800 font-sans">
    <header class="bg-blue-700 text-white p-4 shadow-lg sticky top-0 z-50">
      <nav class="container mx-auto flex justify-between items-center">
        <h1 class="text-2xl font-bold">🔧 AutoFix</h1>
        <div class="space-x-6">
          <a href="#inicio" class="hover:text-yellow-300">Inicio</a>
          <a href="#servicios" class="hover:text-yellow-300">Servicios</a>
          <a href="#contacto" class="hover:text-yellow-300">Contacto</a>
        </div>
      </nav>
    </header>

    <main class="container mx-auto px-4 py-8">
      <slot />
    </main>

    <footer class="bg-gray-900 text-gray-300 text-center py-6 mt-10">
      <p>© {new Date().getFullYear()} AutoFix. Todos los derechos reservados.</p>
    </footer>
  </body>
</html>
